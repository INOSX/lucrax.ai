# dataGPT - Regras de Deploy

## Política de Deploy

### Regra Principal
**TODOS os deploys devem ser realizados PRIMEIRO no ambiente de desenvolvimento. Deploy em produção APENAS quando solicitado explicitamente pelo usuário.**

## Fluxo de Deploy Obrigatório

### 1. Deploy de Desenvolvimento (SEMPRE PRIMEIRO)
```bash
# Deploy automático para desenvolvimento
git push origin develop

# Ou deploy manual para desenvolvimento
vercel --target development
```

### 2. Deploy de Produção (APENAS QUANDO SOLICITADO)
```bash
# Deploy para produção - APENAS quando o usuário solicitar
vercel --prod

# Ou merge para main quando solicitado
git checkout main
git merge develop
git push origin main
```

## Ambientes Configurados

### Desenvolvimento
- **Branch**: `develop`
- **URL**: `https://datagpt-develop.vercel.app`
- **Target**: `development`
- **Configuração**: Variáveis de ambiente de desenvolvimento

### Produção
- **Branch**: `main`
- **URL**: `https://datagpt.vercel.app`
- **Target**: `production`
- **Configuração**: Variáveis de ambiente de produção

## Regras de Deploy

### ✅ Permitido SEMPRE
- Deploy para ambiente de desenvolvimento
- Push para branch `develop`
- Testes em ambiente de desenvolvimento
- Correções em ambiente de desenvolvimento

### ❌ Proibido SEM Solicitação Explícita
- Deploy para produção
- Push para branch `main`
- Merge de `develop` para `main`
- Alteração de variáveis de ambiente de produção

### 🔒 Deploy de Produção - Apenas Quando
- Usuário solicitar explicitamente: "faça deploy para produção"
- Usuário solicitar explicitamente: "deploy em prod"
- Usuário solicitar explicitamente: "merge para main"
- Usuário confirmar: "sim, pode fazer deploy em produção"

## Scripts de Deploy

### Deploy de Desenvolvimento (Automático)
```bash
#!/bin/bash
# deploy-dev.sh
echo "🚀 Fazendo deploy para DESENVOLVIMENTO..."

# Verificar se está no branch develop
current_branch=$(git branch --show-current)
if [ "$current_branch" != "develop" ]; then
    echo "❌ Deploy de desenvolvimento deve ser feito no branch 'develop'"
    echo "💡 Execute: git checkout develop"
    exit 1
fi

# Deploy para desenvolvimento
vercel --target development --yes

echo "✅ Deploy de desenvolvimento concluído!"
echo "🌐 URL: https://datagpt-develop.vercel.app"
```

### Deploy de Produção (Apenas Quando Solicitado)
```bash
#!/bin/bash
# deploy-prod.sh
echo "🚀 Fazendo deploy para PRODUÇÃO..."

# Confirmar se o usuário realmente quer deploy em produção
read -p "⚠️  Tem certeza que deseja fazer deploy em PRODUÇÃO? (digite 'SIM' para confirmar): " confirm
if [ "$confirm" != "SIM" ]; then
    echo "❌ Deploy de produção cancelado"
    exit 1
fi

# Verificar se está no branch main
current_branch=$(git branch --show-current)
if [ "$current_branch" != "main" ]; then
    echo "❌ Deploy de produção deve ser feito no branch 'main'"
    echo "💡 Execute: git checkout main"
    exit 1
fi

# Deploy para produção
vercel --prod --yes

echo "✅ Deploy de produção concluído!"
echo "🌐 URL: https://datagpt.vercel.app"
```

## Configuração do Vercel

### vercel.json - Configuração de Ambientes
```json
{
  "builds": [
    {
      "src": "api/index.py",
      "use": "@vercel/python"
    },
    {
      "src": "index.html",
      "use": "@vercel/static"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/index.py"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ],
  "buildCommand": "pip install -r requirements.txt",
  "outputDirectory": ".",
  "regions": ["iad1"],
  "env": {
    "NODE_ENV": "development"
  }
}
```

### Variáveis de Ambiente por Ambiente

#### Desenvolvimento
```env
# .env.development
NODE_ENV=development
VERCEL_ENV=development
SUPABASE_URL=https://hwfnntgacsebqrprqzzm.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
OPENAI_API_KEY=sk-proj-...
DEBUG=true
LOG_LEVEL=debug
```

#### Produção
```env
# .env.production
NODE_ENV=production
VERCEL_ENV=production
SUPABASE_URL=https://hwfnntgacsebqrprqzzm.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
OPENAI_API_KEY=sk-proj-...
DEBUG=false
LOG_LEVEL=info
```

## Workflow de Desenvolvimento

### 1. Desenvolvimento de Nova Funcionalidade
```bash
# 1. Criar branch de feature
git checkout develop
git pull origin develop
git checkout -b feature/nova-funcionalidade

# 2. Desenvolver e testar
# ... código ...

# 3. Commit e push para feature
git add .
git commit -m "feat: nova funcionalidade"
git push origin feature/nova-funcionalidade

# 4. Merge para develop (deploy automático)
git checkout develop
git merge feature/nova-funcionalidade
git push origin develop  # Deploy automático para desenvolvimento
```

### 2. Deploy para Produção (Apenas Quando Solicitado)
```bash
# 1. Usuário solicita: "faça deploy para produção"
# 2. Verificar se develop está estável
git checkout develop
git pull origin develop

# 3. Merge para main
git checkout main
git pull origin main
git merge develop
git push origin main  # Deploy automático para produção

# 4. Confirmar deploy
echo "✅ Deploy de produção realizado com sucesso!"
```

## Monitoramento de Deploy

### Logs de Deploy
```python
# Adicionar logs específicos para deploy
import logging

def log_deployment(environment: str, version: str, user: str = None):
    """Log de deploy para monitoramento"""
    logger.info(f"🚀 Deploy realizado - Ambiente: {environment}, Versão: {version}")
    
    if environment == "production":
        logger.warning(f"⚠️  Deploy de PRODUÇÃO realizado por: {user or 'Sistema'}")
    
    # Salvar no Supabase se disponível
    if supabase_client.is_connected():
        supabase_client.client.table("api_usage_logs").insert({
            "endpoint": "deploy",
            "method": "POST",
            "status_code": 200,
            "error_message": f"Deploy {environment} - {version}"
        }).execute()
```

### Verificação de Deploy
```python
def verify_deployment(environment: str) -> bool:
    """Verifica se o deploy foi bem-sucedido"""
    try:
        if environment == "development":
            url = "https://datagpt-develop.vercel.app/api/health"
        else:
            url = "https://datagpt.vercel.app/api/health"
        
        response = requests.get(url, timeout=30)
        return response.status_code == 200
        
    except Exception as e:
        logger.error(f"Erro ao verificar deploy {environment}: {e}")
        return False
```

## Checklist de Deploy

### Antes de Qualquer Deploy
- [ ] **Código testado** localmente
- [ ] **Testes unitários** passando
- [ ] **CHANGELOG.md atualizado** com mudanças
- [ ] **Documentação atualizada**
- [ ] **Variáveis de ambiente** configuradas
- [ ] **Branch correto** verificado

### Deploy de Desenvolvimento
- [ ] **Branch develop** ativo
- [ ] **Código commitado** e pushado
- [ ] **Deploy automático** executado
- [ ] **URL de desenvolvimento** verificada
- [ ] **Funcionalidades testadas** no ambiente de desenvolvimento

### Deploy de Produção (Apenas Quando Solicitado)
- [ ] **Solicitação explícita** do usuário
- [ ] **Confirmação** do usuário
- [ ] **Branch main** ativo
- [ ] **Merge de develop** para main
- [ ] **Deploy de produção** executado
- [ ] **URL de produção** verificada
- [ ] **Funcionalidades testadas** no ambiente de produção

## Comandos de Deploy

### Comandos Permitidos Sempre
```bash
# Deploy de desenvolvimento
git push origin develop
vercel --target development

# Verificar status
vercel ls
vercel logs
```

### Comandos que Requerem Solicitação
```bash
# Deploy de produção - APENAS quando solicitado
vercel --prod
git push origin main
git merge develop
```

## Alertas e Notificações

### Deploy de Desenvolvimento
- ✅ Log: "Deploy de desenvolvimento realizado"
- ✅ Notificação: "Ambiente de desenvolvimento atualizado"

### Deploy de Produção
- ⚠️ Log: "ATENÇÃO: Deploy de produção realizado"
- ⚠️ Notificação: "Ambiente de produção atualizado - verificar funcionamento"
- 📧 Email: "Deploy de produção realizado em [timestamp]"

## Rollback

### Rollback de Desenvolvimento
```bash
# Rollback para versão anterior em desenvolvimento
vercel rollback [deployment-url] --target development
```

### Rollback de Produção
```bash
# Rollback para versão anterior em produção - APENAS quando solicitado
vercel rollback [deployment-url] --prod
```

## Resumo das Regras

### ✅ SEMPRE Permitido
1. Deploy para desenvolvimento
2. Push para branch `develop`
3. Testes em ambiente de desenvolvimento
4. Correções em ambiente de desenvolvimento

### ❌ NUNCA Sem Solicitação
1. Deploy para produção
2. Push para branch `main`
3. Merge de `develop` para `main`
4. Alteração de configurações de produção

### 🔒 Deploy de Produção - Apenas Quando
1. Usuário solicitar explicitamente
2. Usuário confirmar a solicitação
3. Usuário usar palavras-chave: "produção", "prod", "main"

---

**Última atualização**: 27/10/2025  
**Versão**: 1.0  
**Política**: Desenvolvimento primeiro, produção apenas quando solicitado
